<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PART2</title>
</head>
<body>
    
<Script type="text/javascript">
    let opcao = prompt("impar ou par?");

   let escolha =  prompt ("escolha um numero de 1 a 9");
 // Converte o valor para número inteiro
    escolha  = parseInt( escolha);

let maquina = Math.floor(Math.random() * (10));

            // Verificação do resultado da partida
            let resultado ;
            let soma = maquina + escolha;
            if ((soma % 2) == 0)
            {resultado = "par"}
            else if((soma % 2) != 0)
            {resultado = "impar"}

            alert("o resulado da partida foi")
            console.log(resultado);
            console.log(opcao);
             if(opcao === "par" && resultado === "par"){
                { alert("Parabéns você venceu!")}
                

            }
            else if(opcao === "impar" && resultado === "impar"){
                { alert("Parabéns você venceu!")}
              

            }
            else
            { alert("A máquina venceu")}




</Script>

</body>
</html>
